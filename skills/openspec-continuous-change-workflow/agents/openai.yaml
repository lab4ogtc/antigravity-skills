interface:
  display_name: "OpenSpec Continuous Change Workflow"
  short_description: "主会话探索决策，独立子代理执行每个变更到总结回传"
  default_prompt: "Use $openspec-continuous-change-workflow to keep the main session as explore/brainstorm/controller only, then delegate one OpenSpec change before new change creation to a dedicated workflow subagent through clean commit, context compression, reload, and summary return."
