interface:
  display_name: "OpenSpec Change Workflow"
  short_description: "人工确认驱动的 OpenSpec 单变更子代理工作流"
  default_prompt: "Use $openspec-change-workflow to run or resume one OpenSpec change with manual Controller confirmation gates and exactly one per-change workflow subagent."
